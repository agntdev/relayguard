[
  {
    "name": "/attach fails in private chat (must be in a group)",
    "steps": [
      {
        "send": { "text": "/attach" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "⚠️ /attach must be used in a group." }
          }
        ]
      }
    ]
  },
  {
    "name": "/detach fails in private chat",
    "steps": [
      {
        "send": { "text": "/detach" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "⚠️ /detach must be used in the moderation group." }
          }
        ]
      }
    ]
  }
]
