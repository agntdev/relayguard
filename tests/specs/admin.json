[
  {
    "name": "/attach fails for non-owner (default user is 1, not the fallback owner)",
    "steps": [
      {
        "send": { "text": "/attach" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "⚠️ You don't have permission to use this command." }
          }
        ]
      }
    ]
  },
  {
    "name": "/detach fails in private chat",
    "steps": [
      {
        "send": { "text": "/detach" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "⚠️ /detach must be used in the moderation group." }
          }
        ]
      }
    ]
  }
]